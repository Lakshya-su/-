print("数字11宽度限制5，结果是：%5d" % num1) #输出：11
print("数字11宽度限制1，结果是：%1d" % num1) #输出：11，如果本身比数字宽度还要小那相当于没写
print("数字11.345宽度限制7，小数精度2，结果是：%7.2f" % num2) #输出：  11.35
print("数字11.345不限制，小数精度2，结果是：%.2f" % num2) #输出：11.35
name="苏涵棋"
money=10000 #f为format的首字母，这种方法不关心类型和精度控制
print(f"我的名字叫{name},我有{money}钱") #输出：我的名字叫苏涵棋，我有10000钱   
表达式：一条具有明确执行结果的代码语句，比如1+1
print("1*1的结果是：%d" % ( 1 *1 ) ) #输出：1*1的结果是1
print(f"1*1的结果是：{ 1 * 1 }") #输出：1*1的结果是1,不是什么时候都需要定义变量的
print("字符串在python中的类型是：%s" %  type("字符串")) #输出：字符串在python中的类型是<class 'str'>
print("请告诉我你是谁：")
name=input()
print("我知道了，你是%s" % name) #输出：我知道了，你是苏涵棋
name=input("请告诉我你是谁：")
print("我知道了，你是%s" % name) #可以在input后面的括号内直接写提示语
num=input("请告诉我你的年龄")
print(f"你的年龄的类型是{type(num)}") #输出：你的年龄的类型是<class 'str'>,说明全当字符串来看待了
num=int(num)
print("你的年龄的类型是",type(num)) #数据类型转化即可
六个比较运算符，和C一致，就不写了
age=18;
if age==18:
    print("我已经成年了")
print("时间过得真快") #注意格式就行，千万不要忘记冒号和四个空格缩进
判断语句的结果必须是布尔类型，True或者False
if age==18:
    print("我已经成年了")
else:
    print("时间过得真快") #if else语句注意格式就行
C中的else if在python中是elif关键字，逻辑不变，格式同if
